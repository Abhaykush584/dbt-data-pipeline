dbt_project:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      port: 5432
      user: postgres
      password: *****
      dbname: weather_data
      schema: analytics
      threads: 1
